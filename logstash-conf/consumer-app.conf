input {
	    file {
	        path => "/tmp/consumer-app*.log"
	        codec => "json"
	        type => "logback"
	    }
	}

	output {
	    if [type]=="logback" {
	         elasticsearch {
	             hosts => [ "localhost:9200" ]
	             index => "consumer-app"
	        }
	    }
	}